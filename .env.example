# IMPORTANTE: Copia este archivo como .env y configura tus valores
# El archivo .env NO se sube a git (está en .gitignore)

# ==============================================================================
# SEGURIDAD - API KEY (OBLIGATORIA)
# ==============================================================================
# Genera una clave segura con: openssl rand -hex 32
# Esta clave protege TODA la aplicación (web + API)
API_KEY=genera-una-clave-super-secreta-y-larga-aqui

# ==============================================================================
# CONFIGURACIÓN DE FLASK
# ==============================================================================
# Modo: development o production
FLASK_ENV=production

# Debug: True o False (SIEMPRE False en producción)
FLASK_DEBUG=False

# ==============================================================================
# RATE LIMITING (Protección contra fuerza bruta)
# ==============================================================================
# Límite de peticiones por minuto (ajusta según tus necesidades)
# Formato: "X per minute" o "X per hour" o "X per day"
RATE_LIMIT_DEFAULT=20 per minute
RATE_LIMIT_STORAGE_URL=memory://
