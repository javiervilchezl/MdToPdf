# IMPORTANTE: Copia este archivo como .env y configura tus valores
# El archivo .env NO se sube a git (está en .gitignore)

# ==============================================================================
# SEGURIDAD - API KEY (OBLIGATORIA)
# ==============================================================================
# Genera una clave segura con: openssl rand -hex 32
# Esta clave protege TODA la aplicación (web + API)
API_KEY=genera-una-clave-super-secreta-y-larga-aqui

# ==============================================================================
# CONFIGURACIÓN DE FLASK
# ==============================================================================
# Modo: development o production
FLASK_ENV=production

# Debug: True o False (SIEMPRE False en producción)
FLASK_DEBUG=False

# ==============================================================================
# RATE LIMITING (Protección contra fuerza bruta)
# ==============================================================================
# Límite general por defecto
RATE_LIMIT_DEFAULT=20 per minute
RATE_LIMIT_STORAGE_URL=memory://

# Límites específicos por endpoint
# Formatos válidos:
#   - "10 per minute" (10 peticiones por minuto)
#   - "100 per hour" (100 peticiones por hora)
#   - "1000 per day" (1000 peticiones por día)
#   - "unlimited" (sin límite - prácticamente ilimitado)
#   - "0" (sin límite - prácticamente ilimitado)

# Interfaz web (navegador)
RATE_LIMIT_WEB_INDEX=20 per minute
RATE_LIMIT_WEB_CONVERT=10 per minute

# API REST
RATE_LIMIT_API_CONVERT=10 per minute
RATE_LIMIT_API_HEALTH=60 per minute

# Ejemplos:
# Sin rate limiting: RATE_LIMIT_API_CONVERT=unlimited
# Límite alto: RATE_LIMIT_API_CONVERT=1000 per hour
# Límite estricto: RATE_LIMIT_API_CONVERT=5 per minute
